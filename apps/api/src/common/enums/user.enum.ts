export enum UserStatus {
  Active = 'active',
  Inactive = 'inactive',
  Draft = 'draft',
  Archived = 'archived',
  Blocked = 'blocked'
}

export enum UserRole {
  User = 'user',
  Client = 'client',
  SuperAdmin = 'superAdmin',
}