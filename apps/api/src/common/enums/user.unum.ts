export enum UserStatus {
  Active = 'active',
  Inactive = 'inactive',
  Draft = 'draft',
  Archived = 'archived',
}

export enum UserRole {
  User = 'user',
  Admin = 'admin',
  SuperAdmin = 'superAdmin',
}