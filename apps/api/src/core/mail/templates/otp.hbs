<!DOCTYPE html>
<html lang="{{language}}">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}}</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: #f5f5f5;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: {
                    {
                    primaryColor
                }
            }

            ;
            padding: 30px 20px;
            text-align: center;
        }

        .logo {
            height: 50px;
            display: block;
            margin: 0 auto;
        }

        .content {
            padding: 40px 30px;
            color: #333;
        }

        .greeting {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 20px;
            color: #111;
        }

        .intro {
            font-size: 16px;
            line-height: 1.5;
            margin-bottom: 30px;
            color: #555;
        }

        .otp-code {
            font-size: 48px;
            font-weight: 700;
            text-align: center;
            letter-spacing: 8px;
            margin: 30px 0;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 8px;

            color: {
                    {
                    primaryColor
                }
            }

            ;
            border: 2px dashed #e9ecef;
        }

        .warning {
            font-size: 14px;
            color: #666;
            margin-top: 30px;
            padding: 15px;
            background: #fff8e6;
            border-radius: 6px;
            border-left: 4px solid #ffc107;
        }

        .footer {
            padding: 25px;
            text-align: center;
            font-size: 12px;
            color: #888;
            border-top: 1px solid #eee;
            background: #fafafa;
        }

        .company-name {
            font-weight: 600;

            color: {
                    {
                    primaryColor
                }
            }

            ;
        }

        .info-box {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-top: 25px;
            font-size: 14px;
            color: #666;
        }

        .info-title {
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Header с логотипом -->
        <div class="header">
            <img src="{{logoUrl}}" alt="{{company}}" class="logo">
        </div>

        <!-- Основное содержимое -->
        <div class="content">
            <div class="greeting">
                {{#if name}}
                {{#if (eq language "ru")}}
                Здравствуйте, {{name}}!
                {{else}}
                Hello, {{name}}!
                {{/if}}
                {{else}}
                {{#if (eq language "ru")}}
                Здравствуйте!
                {{else}}
                Hello!
                {{/if}}
                {{/if}}
            </div>

            <div class="intro">
                {{#if (eq language "ru")}}
                Используйте код ниже для входа на сайт <span class="company-name">{{company}}</span>:
                {{else}}
                Use the code below to log in to the <span class="company-name">{{company}}</span>:
                {{/if}}
            </div>

            <!-- OTP код -->
            <div class="otp-code">{{otp}}</div>

            <!-- Предупреждение -->
            <div class="warning">
                {{#if (eq language "ru")}}
                ⚠️ Не передавайте этот код никому. Срок действия кода: 10 минут.
                {{else}}
                ⚠️ Do not share this code with anyone. Code expires in 10 minutes.
                {{/if}}
            </div>

            <!-- Дополнительная информация -->
            <div class="info-box">
                <div class="info-title">
                    {{#if (eq language "ru")}}
                    Если у вас возникли проблемы:
                    {{else}}
                    If you have any issues:
                    {{/if}}
                </div>
                <ul style="margin: 10px 0 0 0; padding-left: 20px;">
                    <li>
                        {{#if (eq language "ru")}}
                        Убедитесь, что вы запрашивали код входа
                        {{else}}
                        Make sure you requested the login code
                        {{/if}}
                    </li>
                    <li>
                        {{#if (eq language "ru")}}
                        Проверьте правильность email: <strong>{{email}}</strong>
                        {{else}}
                        Check the email address: <strong>{{email}}</strong>
                        {{/if}}
                    </li>
                </ul>
            </div>
        </div>

        <!-- Футер -->
        <div class="footer">
            {{#if (eq language "ru")}}
            © {{year}} {{company}}. Все права защищены.<br>
            Это письмо отправлено автоматически, пожалуйста, не отвечайте на него.
            {{else}}
            © {{year}} {{company}}. All rights reserved.<br>
            This is an automated email, please do not reply.
            {{/if}}
        </div>
    </div>
</body>

</html>